source.uri=file:${user.dir}${file.separator}target${file.separator}source?delete=true&readLock=changed&readLockCheckInterval=500
destination.uri=file:${user.dir}${file.separator}target${file.separator}destination
